# Production Requirements for Vercel Deployment
# Optimized to stay under 50MB deployment limit

# Core
fastapi
uvicorn[standard]
pydantic>=2.5.0
pydantic-settings
pydantic-ai
python-dotenv

# Data fetching
arxiv
feedparser
httpx
aiohttp

# AI/ML - PRODUCTION OPTIMIZED
# sentence-transformers removed - use external API instead
# openai  # Uncomment if using OpenAI embeddings API
# huggingface_hub  # Uncomment if using HuggingFace Inference API

# Database
supabase
sqlalchemy

# Utilities
tenacity
aiofiles

# Audio/TTS
elevenlabs

# Caching
cachetools

# Production optimizations
gunicorn[gevent]>=21.2.0  # Better concurrency for production
psycopg2-binary>=2.9.0    # Faster PostgreSQL adapter (if needed)